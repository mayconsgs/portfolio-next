import styles from "./style.module.scss";

const LoadingTopBar = () => {
  return (
    <div className={styles.loading}>
      <div>
        <div />
      </div>
    </div>
  );
};

export default LoadingTopBar;
